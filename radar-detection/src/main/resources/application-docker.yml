server:
  port: 8081

spring:
  application:
    name: radar-detection
  kafka:
    bootstrap-servers: localhost:9092
  rabbitmq:
    host: rabbitmq
    port: 5672
    username: guest
    password: guest